# ==============================================================================
# Layer8 Security Platform - Configuration Template
# ==============================================================================
# Copy this file to .env and fill in your actual values
# KEEP .env SECURE - Never commit it to version control
# ==============================================================================

# ------------------------------------------------------------------------------
# MySQL Database Configuration
# ------------------------------------------------------------------------------
L8_DB_HOST=your_database_host
L8_DB_PORT=3306
L8_DB_NAME=your_database_name
L8_DB_USER=your_database_user
L8_DB_PASS=your_database_password

# Legacy environment variables (for compatibility)
MYSQL_HOST=your_database_host
MYSQL_PORT=3306
MYSQL_DATABASE=your_database_name
MYSQL_USER=your_database_user
MYSQL_PASSWORD=your_database_password

# Database dialect (mysql or sqlite)
L8_DB_DIALECT=mysql

# ------------------------------------------------------------------------------
# Encryption & Security Configuration
# ------------------------------------------------------------------------------

# Password Pepper (application secret added to passwords before hashing)
# Generate with: openssl rand -base64 32
L8_PEPPER=CHANGE_ME_GENERATE_RANDOM_32_BYTES_BASE64

# Password encryption key (Base64 encoded, 32 bytes when decoded)
# Generate with: openssl rand -base64 32
L8_PWD_KEY_B64=CHANGE_ME_GENERATE_RANDOM_32_BYTES_BASE64

# Password key identifier (for key rotation)
L8_PWD_KEY_ID=k1

# Argon2id password hashing parameters
# Tune these for your server (target: 50-150ms per hash)
L8_ARGON_MEMORY_COST=131072
L8_ARGON_TIME_COST=3
L8_ARGON_THREADS=2

# ------------------------------------------------------------------------------
# Session Configuration
# ------------------------------------------------------------------------------
L8_SESSION_NAME=layer8_admin
L8_SESSION_SECURE=true
L8_SESSION_SAMESITE=Lax

# ------------------------------------------------------------------------------
# Rate Limiting Configuration
# ------------------------------------------------------------------------------
L8_MAX_FAILED_ATTEMPTS=5
L8_LOCKOUT_WINDOW_MIN=15

# ------------------------------------------------------------------------------
# API Keys (Claude AI Integration)
# ------------------------------------------------------------------------------
ANTHROPIC_API_KEY=your_anthropic_api_key_here
CLAUDE_API_KEY=your_claude_api_key_here

# ------------------------------------------------------------------------------
# Application Paths (Auto-detected, can override if needed)
# ------------------------------------------------------------------------------
# L8_DATA_DIR=/path/to/data
# L8_LOG_DIR=/path/to/logs
